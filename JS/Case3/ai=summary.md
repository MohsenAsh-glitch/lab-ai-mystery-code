# CCase 3: The Over-Engineered Mess

:brain: **Prompt used:** *“What does this function do and simplify it.”*
:robot: **AI suggested:** Despite looking “complicated,” it simply computes: a + b. So, y is completely unnecessary (it ends at 0 but is never used), the loop is inefficient for something that can be done in one step, the name complicatedCalc is misleading—it’s actually very simple.
:bulb: **My Insight:** We don't need to overthink! (not always BTW!) 

:white_check_mark: **Fix:** Changed the code into one line. 
