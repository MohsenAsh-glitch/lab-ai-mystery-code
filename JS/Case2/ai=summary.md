# Case 2: The Cryptic Function

:brain: **Prompt used:** *“Explain this function step-by-step and suggest better variable names”*
:robot: **AI suggested:** It checks whether the string is a palindrome (reads the same forward and backward). So having q as a name has no meaning.
:bulb: **My Insight:** To check a string is better to have "===" rather than "==". 

:white_check_mark: **Fix:** Changed the code into one line and added comments. 
